{
  "name": "bot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
 "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
