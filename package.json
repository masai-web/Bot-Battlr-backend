{
  "name": "bot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
 "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
